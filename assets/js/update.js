const dotthree = document.querySelectorAll(".dotthree");
const addUpdateDel = document.querySelectorAll(".addUpdateDel");
const product_item = document.querySelectorAll(".product_item");

array.forEach(element => {
    element
});
dotthree.addEventListener('click', function showFormRegister() {
    addUpdateDel.style.display = 'block';
})

product_item.addEventListener('click', function showFormRegister() {
    addUpdateDel.style.display = 'none';
})